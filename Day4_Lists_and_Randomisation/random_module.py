import random
# below says to pull a random integer between the range of 1 and 10.
random_integer = random.randint(1, 10)
print(random_integer)


# random_float = random.random()
random_float = random.uniform(1.0, 5.0)
print(random_float)
